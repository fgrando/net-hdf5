#ifndef FORMAT_UTILS_H
#define FORMAT_UTILS_H

#include <stdint.h>

inline int load_net_bit(const uint8_t* const buffer, int size, int bit, void* output) {
    int errors = 0;
    
    return errors;
}

inline int load_net_short(uint8_t* buffer, int size, int pos, void* output) {
    int errors = 0;
    
    return errors;
}

inline int load_net_long(uint8_t* buffer, int size, void* output);


#endif